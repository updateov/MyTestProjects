<UserControl x:Class="PatternsCRIClient.Controls.EddDateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="220">
    
    <UserControl.Resources>
        
        <Style x:Key="EDD_DATE_RegularFontStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="Foreground" Value="#1A1A1A" />
        </Style>
        
    </UserControl.Resources>
    
    <Grid>
        <StackPanel Orientation="Vertical">

            <TextBlock Style="{StaticResource EDD_DATE_RegularFontStyle}">EDD:</TextBlock>
            
            <StackPanel Orientation="Horizontal">

                <DatePicker Name="dpEDD" Width="110" Height="30" Style="{StaticResource styleDatePicker}"></DatePicker>

                <TextBlock Name="txtGA" Text="{Binding GestationalAge, StringFormat='GA: {0}'}" 
                           Style="{StaticResource EDD_DATE_RegularFontStyle}" Margin="10,0,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Center" >TEST!!!</TextBlock>

            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
